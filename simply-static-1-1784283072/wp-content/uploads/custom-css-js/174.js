/******* Do not edit this file *******
Simple Custom CSS and JS - by Silkypress.com
Saved: Jul 11 2025 | 09:31:53 */
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const slides = document.querySelectorAll('.main-banner-slider .slide');
    const dots = document.querySelectorAll('.slider-dots .dot');
    let current = 0;
    let startX = 0;
    let endX = 0;

    function showSlide(index) {
      slides.forEach((slide, i) => {
        slide.classList.toggle('active', i === index);
        dots[i].classList.toggle('active', i === index);
      });
      current = index;
    }

    dots.forEach(dot => {
      dot.addEventListener('click', () => {
        const index = parseInt(dot.getAttribute('data-slide'));
        showSlide(index);
      });
    });

    setInterval(() => {
      const next = (current + 1) % slides.length;
      showSlide(next);
    }, 5000);

    const slider = document.querySelector('.main-banner-slider');
    slider.addEventListener('touchstart', e => startX = e.changedTouches[0].clientX);
    slider.addEventListener('touchend', e => {
      endX = e.changedTouches[0].clientX;
      if (startX - endX > 50) showSlide((current + 1) % slides.length);
      else if (endX - startX > 50) showSlide((current - 1 + slides.length) % slides.length);
    });
  });
</script>